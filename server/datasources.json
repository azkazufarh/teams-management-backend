{
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "${DB_URL}",
    "database": "${DB_DATABASE}",
    "password": "${DB_PASSWORD}",
    "name": "${DB_NAME}",
    "user": "${DB_USER}",
    "connector": "${DB_CONNECTOR}"
  }
}